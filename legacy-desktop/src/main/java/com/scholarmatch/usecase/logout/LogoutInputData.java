package com.scholarmatch.usecase.logout;

/**
 * Input data for the logout use case.
 *
 * <p>Empty for now — logout takes no user-supplied parameters — but is kept as its own
 * type (mirroring every other use case's InputBoundary/InputData pair) so the boundary
 * signature would not have to change if a future variant needed one (e.g. "log out of
 * all devices").
 */
public final class LogoutInputData {
}

